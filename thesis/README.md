# Typst Thesis Template

If you need additional typst packages import them in the ```globals.typ```.

## Install fonts

Archlinux:
```ShellSession
$ sudo pacman -S ttf-dejavu
```

MacOS:
```ShellSession
$ brew install --cask font-dejavu
```
